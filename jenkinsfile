pipeline {
    agent any
    tools {
        nodejs 'Nodejs22-6-0'
    }

    stages {
        stage('installing dependencies') {
            steps {
                sh 'npm install --no-audit'
            }
        }
        stage('Debug OWASP Installation') {
    steps {
        script {
            sh '''
            echo "Checking OWASP Dependency Check path..."
            which dependency-check.sh
            find /var/lib/jenkins/tools/ -name dependency-check.sh
            '''
        }
    }
}


        stage('npm-dependency_parallel') {
            parallel {
                stage('npm dependency check') {
                    steps {
                        sh 'npm audit --audit-level=critical'
                    }
                }

                stage('OWASP Dependency Check') {
                    steps {
                        script {
                            sh '''
                            dependency-check.sh --project "MyProject" \
                            --scan ./ \
                            --format ALL \
                            --out ./ \
                            --prettyPrint
                            '''
                        }
                    }
                }
            } // ✅ Closing parallel block correctly
        }

        stage('Test') {
            steps {
                echo 'Testing the app'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the app'
            }
        }
    }
}
